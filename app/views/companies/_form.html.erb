
<%= form_for @company do |f| %>
  <div class="field">
    <%= f.label I18n.t('views.company.fields.name') %><br>
    <%= f.text_field :name %>
  </div>
 
  <div class="actions">
    <%= f.submit I18n.t("views.common.submit")%>
  </div>
  
<% end %>